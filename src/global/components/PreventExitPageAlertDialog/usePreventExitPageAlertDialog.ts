'use client'

import { useEventListener } from '@/global/hooks/useEventListener'

export function usePreventExitPageAlertDialog(
  openAlertDialog: VoidFunction | null,
  isEnabled: boolean
) {
  // useEventListener('p')
}

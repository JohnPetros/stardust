{
  "name": "stardust",
  "description": "Application for language programming learning",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@10.8.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3"
  }
}